# 引継ぎ書 — 九大ギルドアプリ

作成日: 2026-05-20  
ブランチ: `fix/ui-api-issues`（main に未マージ）

---

## このドキュメントの目的

AIによるコードレビューで見つかった問題のうち、**第一弾の修正（fix/ui-api-issues ブランチ）では対応しきれなかった残課題**を次の担当者（AI・人間どちらでも）が迷わず着手できるよう整理したもの。

---

## プロジェクト概要

**九州大学の冒険者ギルド** をテーマにしたメンバー管理・クエスト管理アプリ。

- **フロントエンド**: Next.js 16.2.4 / React 19 / Tailwind CSS 4 / Framer Motion
- **バックエンド**: Next.js API Routes（サーバーサイド）
- **DB**: Supabase（PostgreSQL）
- **認証**: NextAuth.js + Discord OAuth
- **主なファイル**:
  - `src/app/page.tsx` — トップページ
  - `src/components/member/MemberCard.tsx` — 会員証カード
  - `src/components/ui/Header.tsx` — ヘッダー
  - `src/components/stamp/StampCard.tsx` — スタンプカード / チェックイン
  - `src/contexts/GuildContext.tsx` — アプリ全体の状態管理
  - `src/app/api/` — API ルート群

---

## 第一弾で対応済みの修正（fix/ui-api-issues ブランチ）

以下はすでに修正・コミット済み。触らなくてよい。

| 修正 | ファイル |
|---|---|
| モバイルでヒーローのコピーが見切れる | `src/app/page.tsx` |
| 会員証カードの `role="button"` によるボタン入れ子問題 | `src/components/member/MemberCard.tsx` |
| プロフィール編集パネルの Esc 対応 / × ボタンの aria-label / role="dialog" | `src/components/member/MemberCard.tsx` |
| leaderboard / achievements / stats API の 500 エラー → 空配列縮退 | `src/app/api/member/*/route.ts` |

---

## 残課題一覧

### 優先度：高（セキュリティ）

---

#### S1. レビュー投稿 API がログイン不要

**ファイル**: `src/app/api/review/route.ts`  
**問題**: 認証チェックが一切ない。誰でも任意のメンバー名で評価を POST できる。  
**影響**: 見知らぬ人に勝手に実績・評価を書かれる。低評価や偽実績でプロフィールが汚染される。

**修正方針**:
```ts
// POST の先頭に追加
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';

const session = await getServerSession(authOptions);
if (!session?.user) {
  return NextResponse.json({ error: 'ログインが必要です' }, { status: 401 });
}
```

---

#### S2. Discord 通知 API がログイン不要

**ファイル**: `src/app/api/notify/route.ts`  
**問題**: 認証チェックがない。誰でも Webhook に通知を送れる。  
**影響**: 無関係な通知が Discord に流れ、本物のチェックイン通知が埋もれる。

**修正方針**: S1 と同じく POST の先頭に `getServerSession` によるセッションチェックを追加する。

---

#### S3. デバッグ API が本番公開されている

**ファイル**: `src/app/api/debug-session/route.ts`  
**問題**: `/api/debug-session` に GET すると、ログイン中ユーザーの Discord ID・名前・ロール情報がそのまま JSON で返ってくる。  
**影響**: ユーザーが自分のセッション情報を見られる。悪用される可能性がある。

**修正方針（2択）**:
- **案A（推奨）**: ファイルごと削除する（デバッグ用途は終わっているはず）
- **案B**: 環境変数 `NODE_ENV !== 'development'` の場合は 404 を返す

---

#### S4. チェックイン回数がフロント側で管理されている（改ざん可能）

**ファイル**: `src/contexts/GuildContext.tsx`（`monthlyCheckInCount` state）  
**ファイル**: `src/app/api/profile/route.ts`（チェックイン回数を POST で受け取って保存）

**問題**: チェックイン回数のカウントアップはフロント側で行われ、POST で DB に書き込まれる。フロントの state を操作すれば、実際に拠点に来ていなくてもカウントを増やせる。

**影響**: ランキングの信頼性が失われる（不正に順位を上げられる）。

**修正方針**:  
チェックイン回数のカウントアップを **API 側（サーバー）** に移す。
- 「チェックイン」という明確な API エンドポイントを新設（`/api/checkin`）
- 1日1回・同一 Discord ID からのみカウントを +1 する制限をサーバー側で実装
- フロントからは「カウント値」を送らず、「チェックインした」というリクエストだけを送る

---

### 優先度：中（UX改善）

---

#### U1. 通知ベルボタンを押しても何も起きない

**ファイル**: `src/components/ui/Header.tsx`（91〜97行目）  
**問題**: `<button id="header-notification-btn">` に `onClick` ハンドラが存在しない。ボタンを押しても視覚的な変化も機能もない。  
**影響**: ユーザーが「壊れている」と感じる。

**修正方針（2択）**:
- **案A（機能を作る）**: プッシュ通知の許可ダイアログを出すか、通知設定パネルを開く処理を追加。状態変化をトースト表示や色変化で明示する。
- **案B（暫定）**: 機能が未実装の間はボタンを非表示にするか、`disabled` + ツールチップ「近日公開」で対応する。

---

#### U2. モバイルでヘッダーの情報が省略されすぎて意味不明

**ファイル**: `src/components/ui/Header.tsx`  
**問題**:
- ロゴ横の「九大ギルド」テキストが `hidden sm:block` のためスマホで非表示
- Wi-Fi ボタンは `title` 属性でのみ説明しているが、スマホでは tooltip が出ない

**修正方針**:
- 「九大ギルド」を常時表示にする（`hidden sm:block` → 常時表示。文字サイズを調整）
- Wi-Fi ボタンの状態を色・アイコンだけでなく、短いテキストラベルでも表示する（スマホでは `text-[10px]` 等で小さく添える）

---

#### U3. Achievements タブが毎回「読み込み中...」から始まる

**ファイル**: `src/components/member/MemberCard.tsx`（`CardBack` コンポーネント、289〜300行目）  
**問題**:
- タブを切り替えるたびに毎回 fetch が走り、「読み込み中...」が表示される
- データが存在しない場合でも、fetch が完了するまで「読み込み中...」が表示される

**修正方針**:
```ts
// 一度取得したらキャッシュする
const [fetched, setFetched] = useState(false);

useEffect(() => {
  if (tab === 'achievements' && !fetched && member.name !== '冒険者') {
    setLoadingAchievements(true);
    fetch(...)
      .then(...)
      .finally(() => { setFetched(true); setLoadingAchievements(false); });
  }
}, [tab, member.name, fetched]);
```

---

### 優先度：低（DB作業 / 環境構築）

これらはコードではなく Supabase の SQL Editor での作業が必要。詳細は `SETUP.md` を参照。

| # | 作業内容 | 参照ファイル |
|---|---|---|
| D1 | `supabase_schema.sql` を実行（`quest_completions` / `evaluations` テーブル作成） | `supabase_schema.sql` |
| D2 | `supabase_migration.sql` を実行（`member_profiles` にカラム追加） | `supabase_migration.sql` |
| D3 | `member_profiles` テーブルが存在するか確認。なければ作成 | `SETUP.md` の A-1 節 |
| D4 | `get_skill_stats` RPC の作成（任意。なくてもフォールバックが動く） | `src/app/api/member/stats/[name]/route.ts` |

---

## 作業を始める前に

### ローカル環境の立ち上げ方

```bash
# リポジトリをクローン（既にある場合はスキップ）
git clone https://github.com/teruk328-design/-.git guild-app
cd guild-app

# fix/ui-api-issues ブランチに切り替え
git checkout fix/ui-api-issues

# 依存関係のインストール
npm install

# 環境変数ファイルを作成（SETUP.md の B-1 節を参照）
# .env.local に Supabase の実値 or ダミー値を設定

# 開発サーバー起動
npm run dev
```

### ブランチ運用ルール
- **main には直接 push しない**
- 修正は `fix/ui-api-issues` を継続して使うか、新しいブランチを切って PR を出す
- PR は GitHub の `https://github.com/teruk328-design/-` から作成

---

## 関連ドキュメント

| ファイル | 内容 |
|---|---|
| `SETUP.md` | Supabase チェックリスト + ローカル確認手順 |
| `修正内容.md` | 第一弾で直した内容の非エンジニア向け説明 |
| `Vercelデプロイ手順.md` | Vercel でブランチを公開する手順 |
| `AIによる問題点.md` | 今回の修正の起点となったレビューメモ（このファイルはリポジトリ外） |
| `supabase_schema.sql` | DB テーブル定義 |
| `supabase_migration.sql` | `member_profiles` への追加マイグレーション |
